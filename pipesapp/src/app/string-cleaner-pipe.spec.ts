import { StringCleanerPipe } from './string-cleaner-pipe';

describe('StringCleanerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringCleanerPipe();
    expect(pipe).toBeTruthy();
  });
});
